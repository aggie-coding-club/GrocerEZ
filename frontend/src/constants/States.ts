export enum headerStates {
    title,
    search,
    storeSelect
}

export enum bottomStates {
    none,
    itemSelection,
    total
}

export enum pageStates {
    none,
    itemList,
    itemListDialogue,
    itemListPrices,
    searchResults,
    searchFilters,
    loading
}

export enum globalStates {
    listSelection,
    searchQuery,
    searchFilter,
    searchResults,
    optionsDialog,
    loadingScreen,
    finalListSelection
}