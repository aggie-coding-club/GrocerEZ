export enum HeaderStates {
    title,
    search,
    storeSelect
}

export enum BottomStates {
    none,
    itemSelection,
    total
}

export enum PageStates {
    none,
    itemList,
    itemListPrices,
    searchResults,
    searchFilters,
    loading
}

export enum GlobalStates {
    listSelection,
    searchQuery,
    searchFilter,
    searchResults,
    loadingScreen,
    finalListSelection
}